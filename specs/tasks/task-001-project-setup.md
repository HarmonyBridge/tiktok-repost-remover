# Task 001: Project Setup

## Objective
Initialize the React Native + Expo project with all required dependencies.

## Steps
1. Create project with TypeScript template
2. Install core dependencies (expo-router, webview, reanimated, etc.)
3. Configure expo-router and entry point
4. Setup TypeScript strict mode
5. Create folder structure

## Checklist
- [x] Project created with `npx create-expo-app`
- [x] All dependencies installed
- [x] expo-router configured
- [x] tsconfig.json has strict mode enabled
- [x] Folder structure matches plan

## Status: ✅ Completed
